CREATE DATABASE starwars;
# Cria um banco de dados e se ele já existir é gerado um erro, informando que o db já existe.

CREATE DATABASE IF NOT EXISTS starwars;
# Cria um banco de dados caso esse banco ainda não exista.